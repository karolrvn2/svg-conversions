{
  "name": "svg-conversion",
  "version": "1.0.0",
  "description": "Convert SVG colors in Rust/Wasm Web Workers",
  "type": "module",
  "exports": "./web/svg-worker-pool.js",
  "scripts": {
    "build": "wasm-pack build --target web --release --out-dir pkg",
    "prepare:icons": "node scripts/prepare-icons.mjs",
    "build:demo": "npm run build && npm run prepare:icons && trunk build demo/index.html --release --dist dist",
    "start:demo": "npm run build && npm run prepare:icons && trunk serve demo/index.html --port 8055 --open",
    "test": "cargo test",
    "check": "cargo check --all-targets && cargo check --target wasm32-unknown-unknown"
  },
  "author": "",
  "license": "ISC",
  "files": ["pkg", "web"]
}
